# API base URL (no trailing slash). Use your PC's LAN IP so the phone can reach WAMP.
# On Windows: run "ipconfig" and use the IPv4 Address (e.g. 192.168.0.100).
# Copy this file to .env and set your IP, then restart Expo (npx expo start).
EXPO_PUBLIC_API_URL=https://pcstech.in/catering/backend/index.php/api
